name: Deploy myapp

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted          # matches --labels set in step 4

    steps:
      - name: Rebuild and redeploy
        run: |
          cd /home/devops-user/myapp/
          docker compose down
          docker compose up -d --build
